import { ExternalLoginDirective } from './external-login.directive';

describe('ExternalLoginDirective', () => {
	it('should create an instance', () => {
		const directive = new ExternalLoginDirective();
		expect(directive).toBeTruthy();
	});
});
